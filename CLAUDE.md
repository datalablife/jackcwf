
[项目记忆规则]
        - **必须主动调用** progress-recorder agent 来记录重要决策、任务变更、完成事项等关键信息到progress.md
        - 检测到以下情况时**立即自动触发** progress-recorder:
               *   出现"决定使用/最终选择/将采用"等决策语言
               *   出现"必须/不能/要求"等约束语言
               *   出现"完成了/实现了/修复了"等完成标识
               *   出现"需要/应该/计划"等新任务
        - 当 progress.md 的 Notes/Done 条目过多 (>100条)影响阅读时，应归档到 progress.archive.md

[指令集 - 前缀 "/"]
        - record：使用 progress-recorder 执行增量合并任务
        - archive：使用 progress-recorder 执行快照归档任务
        - recap：阅读 progress.md，回顾项目当前状态（包括但不仅限于关键约束、待办事项、完成时度等）

[代码模块索引]
        - 需要理解 `src/` 结构时，优先查阅 `docs/guides/MODULE_OVERVIEW.md`。该文档按 FastAPI 应用、API 路由、服务、仓储、中间件与工具等模块总结功能，便于 CLAUDE code 快速定位实现。

[代码开发工作流 - 模块文档更新规则]
        - **自动Hook检查（推荐）** ✅
               * 任务完成后，Hook 会自动检测 src/ 目录的代码变化
               * 如有变化，Hook 会友好地提示用户是否需要更新模块文档
               * Hook 工作流详见 `.claude/HOOKS_SETUP_GUIDE.md`
        - **必须在任何 src/ 目录代码变更完成后** 立即执行 /update-module-docs 命令更新模块文档
        - **必须在生成总结文档前** 确保 docs/guides/MODULE_OVERVIEW.md 已同步更新
        - 检测到以下情况时**必须立即触发文档更新**:
               * 在 src/ 目录下创建了新的 .py 文件（新模块、新服务、新API路由）
               * 修改了现有模块的函数签名、类定义或核心功能
               * 删除或重命名了模块中的关键功能、类或函数
               * 添加了新的 API 路由或服务端点
               * 修改了数据库模型或仓储层
        - 模块文档更新内容应包含:
               * 新增功能的详细说明和用途
               * 修改的接口/函数签名（包括参数、返回值）
               * 相关代码的文件路径和行号引用（格式: file_path:line_number）
               * 删除的过时模块说明（如适用）
               * 模块间的依赖关系和调用关系（如有变化）
        - 使用工具: 优先使用 /update-module-docs 命令，通过该命令自动读取、分析和更新文档

[文件输出规则改进记录]
        - **2025-11-17 v2.3**: Hook脚本最终完美优化
               * ✅ 正确区分报告文档vs纯粹状态标记
               * ✅ EPIC_*前缀文件识别为报告（docs/reference/）
               * ✅ _COMPLETION_/_IMPLEMENTATION_/_REMEDIATION_识别为报告
               * ✅ FINAL、SUMMARY词识别为报告文档
               * ✅ 6个Epic完成报告正确归档到docs/reference/
               * ✅ is_priority1_file和get_target_location逻辑完全同步
               * ✅ 所有29个文档文件100%正确分类

        - **2025-11-17 v2.2**: Hook脚本最终优化完成
               * .txt文件支持
               * 无优先级关键词文件默认规则
               * CURRENT_STATUS→docs/reference等特定文件映射

        - **2025-11-17 v2.1**: 状态标记优先级判断改进
               * SUCCESS/COMPLETE优先保留在根目录（后被v2.3纠正）
               * 特殊规则：REPORT优先于SUCCESS

        - **2025-11-17 v2.0**: 增强Hook脚本规则初版
               * 双向文件整理（根目录↔docs/）
               * 智能优先级判断处理关键词冲突
               * 根目录扫描功能

        - 规则定义（v2.3最终版）：
               * Priority 1 (根目录): 纯粹的项目状态标记 (PROJECT_*, SYSTEM_*等简短标记)
               * Priority 2 (docs/): 所有详细报告 (EPIC_*, *_COMPLETION_, *_FINAL等)
               * 特殊规则: EPIC_前缀、FINAL、SUMMARY、_IMPLEMENTATION_等→docs/reference/

## 项目启动 FastAPI 应用

### 后端服务器
先杀死8000端口占用的进程：
Bash(pkill -f "python.*src.main" 2>/dev/null; sleep 1; echo "清理旧进程完成")

然后再启动后端服务器：
 source .venv/bin/activate && python -m uvicorn src.main:app --host 0.0.0.0 --port 8000

### 前端服务器
 cd /mnt/d/工作区/云开发/working/frontend && npm run dev -- --host 0.0.0.0

## 📊 前端架构全景图

```
┌─────────────────────────────────────────────────────────────────┐
│                    Frontend Architecture                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │           React Components Layer (UI/UX)                 │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  • ChatInterface (消息展示)                               │   │
│  │  • ChatInput (消息输入)                                  │   │
│  │  • Sidebar (线程列表)                                    │   │
│  │  • ChatMessage (单条消息)                                │   │
│  │  • TypingIndicator (输入指示)                            │   │
│  │  • ToolRenderer (工具展示)                               │   │
│  │  • ExportMenu (导出功能)                                 │   │
│  │  • ThemeToggle (深色模式)                                │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ▲                                        │
│                          │ (Props & Events)                       │
│                          │                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │        State Management Layer (Zustand + Persist)       │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  • ChatStore (messages, typingUsers, cacheMetrics)       │   │
│  │  • ThreadsStore (threads, selectedThreadId)             │   │
│  │  • UIStore (theme, sidebarOpen, debugMode)              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ▲                                        │
│                          │ (Subscriptions)                        │
│                          │                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │          Custom Hooks Layer (Business Logic)            │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  • useChat (消息发送、流式处理)                          │   │
│  │  • useThread (线程操作、CRUD)                            │   │
│  │  • useAutoTitle (标题生成)                               │   │
│  │  • useTypingIndicator (WebSocket 输入指示)              │   │
│  │  • useWebSocket (实时连接)                               │   │
│  │  • Custom Hooks (8+ hooks)                               │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ▲                                        │
│                          │ (API Calls)                            │
│                          │                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         API Layer (Axios + Error Handling)              │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  • conversationApi (会话 CRUD + 标题生成)                │   │
│  │  • threadApi (线程 API + 状态获取)                       │   │
│  │  • messageApi (消息 CRUD)                                │   │
│  │  • documentApi (文档搜索和上传)                          │   │
│  │  • streamingApi (SSE 流式通信)                           │   │
│  │  • healthApi (健康检查)                                  │   │
│  │  • Interceptors: Auth token, 401 refresh, 502/503 retry │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ▲                                        │
│                          │                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │    Backend API Server (FastAPI + WebSocket)             │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  Base URL: http://localhost:8000/api/v1                 │   │
│  │  Timeout: 30s | Auth: Bearer Token                       │   │
│  │  WebSocket: ws://localhost:8000/ws                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```


## 🔧 LangChain Application 项目生产环境

部署在服务器(47.79.87.199)上，利用coolify部署生产容器，其容器相关信息如下：
  New Application (jackcwf-fastapi-v2)

  | Property     | Value                              |
  |--------------|------------------------------------|
  | UUID         | l8c0sw8w4o8sscgww88gw4gg           |
  | Name         | jackcwf-fastapi-v2                 |
  | Status       | ✅ running:healthy                  |
  | FQDN         | https://chat.jackcwf.com           |
  | Docker Image | ghcr.io/datalablife/jackcwf:latest |
  | Branch       | main  

  Environment Variables Configured (7 total)：
  ✓ ENVIRONMENT = production
  ✓ LOG_LEVEL = info
  ✓ PYTHONUNBUFFERED = 1
  ✓ PYTHONDONTWRITEBYTECODE = 1
  ✓ DATABASE_URL = postgresql+asyncpg://jackcwf888:Jack_00492300@47.79.87.199:5432/postgres
  ✓ PORT = 80
  ✓ HOST = 0.0.0.0

## 🔧 Technical Architecture

### System Components

┌─────────────────────────────────────────────────────────┐
│               LangChain Application                       │
│          (AI Conversation Backend)                        │
└──────────────────┬──────────────────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
   ┌────────────┐      ┌─────────────┐
   │  LangChain │      │   RAG API   │
   │  Agents    │      │  Endpoints  │
   └──────┬─────┘      └──────┬──────┘
          │                   │
          └─────────┬─────────┘
                    │
          ┌─────────▼──────────┐
          │  Python asyncpg    │
          │  (Connection Pool) │
          └─────────┬──────────┘
                    │
          ┌─────────▼──────────────────────┐
          │  Coolify PostgreSQL Server      │
          │  IP: 47.79.87.199:5432         │
          │                                │
          │  ┌──────────────────────────┐ │
          │  │  Lantern Extension       │ │
          │  │  - HNSW Vector Index     │ │
          │  │  - Vector Operations     │ │
          │  │  - Similarity Search     │ │
          │  └──────────────────────────┘ │
          │                                │
          │  ┌──────────────────────────┐ │
          │  │  PostgreSQL 15.8         │ │
          │  │  - Documents Table       │ │
          │  │  - Conversations Table   │ │
          │  │  - Search History        │ │
          │  │  - Job Queue             │ │
          │  └──────────────────────────┘ │
          └────────────────────────────────┘

### Data Flow: RAG Query Pipeline

User Query
    │
    ▼
┌─────────────────────┐
│ 1. Encode Query     │
│ (Embedding Model)   │
└──────┬──────────────┘
       │ 1,536-dim vector
       ▼
┌──────────────────────────────┐
│ 2. Search Similar Documents  │
│ (Lantern HNSW Index)         │
│ Query: embedding <-> query   │
└──────┬───────────────────────┘
       │ Top 5 results with distances
       ▼
┌──────────────────────────────┐
│ 3. Retrieve Context          │
│ (Join with metadata)         │
│ Filter by document_type,     │
│ source, date range, etc.     │
└──────┬───────────────────────┘
       │ Rich context documents
       ▼
┌──────────────────────────────┐
│ 4. Generate Response         │
│ (LLM + Context)              │
│ LangChain Chains/Agents      │
└──────┬───────────────────────┘
       │ Generated answer
       ▼
┌──────────────────────────────┐
│ 5. Store in History          │
│ search_history table         │
│ For analytics & optimization │
└──────┬───────────────────────┘
       │
       ▼
    Return to User

### 代码架构

src/
  ├── main.py                          # FastAPI 应用入口（Phase 1/3 增强）
  ├── db/
  │   ├── config.py                    # SQLAlchemy + pgvector 配置
  │   ├── migrations.py                # 数据库初始化与分区
  │   └── base.py                      # ORM 基类
  ├── models/                          # 4 个 ORM 模型 + streaming models
  ├── repositories/                    # 4 个异步存储库
  ├── api/
  │   ├── conversation_routes.py       # 会话 CRUD + 聊天端点（Phase 1 增强）
  │   ├── message_routes.py            # 消息端点
  │   ├── document_routes.py           # 文档上传、搜索、删除
  │   ├── websocket_routes.py          # 实时 WebSocket 通信
  │   ├── tools_routes.py              # Agent 工具列表与执行
  │   ├── streaming_routes.py          # SSE 流式端点（Phase 2）
  │   ├── cache_admin_routes.py        # 缓存管理 API（Phase 1）
  │   └── claude_cache_routes.py       # Claude 缓存管理 API（Phase 3）
  ├── services/
  │   ├── embedding_service.py         # OpenAI 嵌入（Epic 2）
  │   ├── document_service.py          # 文档处理与分块（Epic 2）
  │   ├── agent_service.py             # LangChain Agent（Epic 2）
  │   ├── conversation_summarization_service.py  # 对话总结（Epic 2）
  │   ├── cached_rag.py                # 语义缓存 RAG（Phase 1）
  │   ├── semantic_cache.py            # 缓存服务（Phase 1 增强）
  │   ├── streaming_chat_service.py    # 流式聊天（Phase 2）
  │   ├── claude_cache_manager.py      # Claude 缓存（Phase 3）
  │   ├── claude_integration.py        # Claude 集成（Phase 3）
  │   └── middleware/
  │       ├── cost_tracking.py         # 成本追踪中间件
  │       └── memory_injection.py      # 记忆注入中间件
  ├── middleware/                      # 5 层中间件系统（Epic 3）
  ├── infrastructure/
  │   ├── health.py                    # 健康检查（Epic 3）
  │   ├── shutdown.py                  # 优雅关闭（Epic 3）
  │   ├── cache_metrics.py             # Prometheus 指标（Phase 1）
  │   ├── cache_stats_updater.py       # 缓存统计更新（Phase 1）
  │   ├── monitoring.py                # 监控（Phase 2）
  │   └── claude_cost_tracker.py       # Claude 成本追踪（Phase 3）
  └── patterns/
      └── circuit_breaker.py           # 电路断路器（Epic 3）

---

### 质量指标
  | 维度            | 目标         | 实际      | 状态     |
  |---------------|------------|---------|--------|
  | 代码质量          | 8.0/10     | 9.2/10  | ✅ +15% |
  | 测试覆盖          | 80%        | 88%+    | ✅ +10% |
  | 测试通过率         | 100%       | 100%    | ✅ 完美   |
  | 性能改进          | 目标值        | +30-85% | ✅ 超目标  |
  | 向量搜索          | ≤200ms P99 | ≤200ms  | ✅ 达标   |
  | API响应         | <350ms P50 | <350ms  | ✅ 达标   |
  | type coverage | 80%        | 100%    | ✅ 完美   |
  | 文档完整度         | 80%        | 100%    | ✅ 完美   |
---

## 📍 您的 Coolify PostgreSQL 信息

根据 `.env` 文件中的配置：

| 配置项 | 值 |
|--------|-----|
| **应用 UUID** | ok0s0cgw8ck0w8kgs8kk4kk8 |
| **应用名** | docker-image-ok0s0cgw8ck0w8kgs8kk4kk8 |
| **FQDN** | https://pgvctor.jackcwf.com |
| **用户名** | jackcwf888 |
| **端口** | 5432 |
| **数据库** | postgres |

⚠️ **重要**: 密码存储在 `.env` 文件中，不在本指南中显示

---

### 📋 获取实际的 PostgreSQL 连接信息

### 方法 1: 通过 Coolify 网页界面

```
1. 访问 https://coolpanel.jackcwf.com
2. 使用凭证登录
3. 导航到应用管理
4. 找到 PostgreSQL 应用 (ok0s0cgw8ck0w8kgs8kk4kk8)
5. 查看"连接信息"或"环境变量"
6. 复制 POSTGRES_HOST 的实际值（IP 地址或域名）
7. 确认端口（通常是 5432）
```

### 方法 2: 通过 Coolify CLI

```bash
# 获取应用详细信息（需要 Coolify CLI 配置）
coolify app show ok0s0cgw8ck0w8kgs8kk4kk8 --show-sensitive --format json

# 查找 POSTGRES_HOST 字段中的实际主机值
```

### 方法 3: 测试连接

```bash
# 使用 psql 测试连接（需要安装 PostgreSQL 客户端）
psql -h <POSTGRES_HOST> -U jackcwf888 -d postgres -c "SELECT version();"

# 系统会提示输入密码
# 如果连接成功，您将看到 PostgreSQL 版本信息
```

---

### ✅ 验证 pgvector 扩展

pgvector 是向量搜索所必需的：

```sql
-- 登录到 PostgreSQL
psql -h <POSTGRES_HOST> -U jackcwf888 -d postgres

-- 检查是否已安装
SELECT extname FROM pg_extension WHERE extname = 'vector';

-- 如果未安装，创建扩展（需要超级用户权限）
CREATE EXTENSION IF NOT EXISTS vector;

-- 验证
SELECT version FROM pg_extension WHERE extname = 'vector';
```

---

### 📦 依赖包管理指南

## 工具选择策略

本项目支持两种依赖包管理工具，CLAUDE 应根据以下场景进行选择：

### **优先使用 UV（推荐）**
- **优点**：极快的安装速度、内存占用低、内置虚拟环境管理
- **场景**：
  - 快速安装依赖（开发、测试、部署）
  - 在资源受限的环境中运行
  - CI/CD 流程（GitHub Actions、Docker 构建）
  - 快速迭代开发

### **使用 Poetry（备选）**
- **优点**：完整的项目依赖锁定、依赖冲突解决、发布管理
- **场景**：
  - 需要完整的依赖版本锁定（poetry.lock）
  - 发布项目到 PyPI
  - 复杂的依赖冲突需要解决

---

### 📋 UV 使用指南（推荐）

#### 安装 UV
```bash
# 在 macOS 和 Linux 上安装
curl -LsSf https://astral.sh/uv/install.sh | sh

# 或通过包管理器（Linux）
apt-get install uv  # Debian/Ubuntu
brew install uv     # macOS

# Windows（需要 PowerShell）
powershell -ExecutionPolicy BypassUser -c "irm https://astral.sh/uv/install.ps1 | iex"
```

#### UV 基本命令

**1. 创建虚拟环境（可选，UV 会自动创建）**
```bash
uv venv                    # 创建 .venv
source .venv/bin/activate  # 激活（Linux/macOS）
.venv\Scripts\activate     # 激活（Windows）
```

**2. 安装依赖 - 场景指南**

```bash
# 场景 1: 开发环境（包含所有依赖 + 开发工具）
uv pip install -e ".[dev,test]"

# 场景 2: 生产环境（仅运行时依赖）
uv pip install -e "."

# 场景 3: 测试环境（运行时 + 测试工具）
uv pip install -e ".[test]"

# 场景 4: 快速尝试（不安装项目，仅安装依赖）
uv pip install -r requirements.txt  # 如果有 requirements.txt
uv pip install fastapi uvicorn sqlalchemy pytest pytest-asyncio

# 场景 5: 更新依赖到最新版本
uv pip install --upgrade -e ".[dev,test]"

# 场景 6: 查看已安装的依赖
uv pip list
uv pip show <package-name>
```

**3. 运行测试**
```bash
# 场景 1: 运行所有测试
uv run pytest tests/ -v

# 场景 2: 运行特定测试文件
uv run pytest tests/test_story33_performance.py -v

# 场景 3: 运行特定测试类或方法
uv run pytest tests/test_story33_performance.py::TestPerformance::test_rag_search -v

# 场景 4: 运行测试并生成覆盖率报告
uv run pytest tests/ --cov=src --cov-report=html

# 场景 5: 运行 E2E 测试
uv run pytest tests/test_story33_e2e_integration.py -v --tb=short
```

**4. 运行应用**
```bash
# 开发环境（带热重载）
uv run uvicorn src.main:app --reload --host 0.0.0.0 --port 8000

# 生产环境
uv run gunicorn -w 4 -k uvicorn.workers.UvicornWorker src.main:app
```

---

### 📦 Poetry 使用指南（备选）

#### 安装 Poetry
```bash
# 推荐方式（一行安装）
curl -sSL https://install.python-poetry.org | python3 -

# macOS
brew install poetry

# 验证安装
poetry --version
```

#### Poetry 基本命令

**1. 初始化项目（如果需要）**
```bash
poetry init
```

**2. 安装依赖**
```bash
# 场景 1: 完整安装（包含所有依赖）
poetry install

# 场景 2: 仅安装生产依赖（不安装开发依赖）
poetry install --no-dev

# 场景 3: 更新依赖到最新兼容版本
poetry update

# 场景 4: 添加新依赖
poetry add fastapi          # 生产依赖
poetry add pytest --group dev  # 开发依赖
```

**3. 运行测试和命令**
```bash
# 运行测试
poetry run pytest tests/ -v

# 运行特定应用
poetry run uvicorn src.main:app --reload

# 进入 poetry 环境的 Python shell
poetry shell
python  # 现在可以直接使用项目的依赖
```

**4. 锁定依赖版本**
```bash
# 生成 poetry.lock（所有依赖的精确版本）
poetry lock

# 查看依赖树
poetry show --tree
```

---

### 🎯 场景决策树

```
┌─────────────────────────────────────────┐
│      需要管理项目依赖？                  │
└──────────────┬──────────────────────────┘
               │
       ┌───────┴───────┐
       │               │
       ▼               ▼
    只想    需要完整依赖
   快速    版本锁定？
   安装？
       │               │
      UV            Poetry
   （推荐）         （备选）
       │               │
    ┌──┴──────┐        │
    │快速✓    │        │
    │简单✓    │        │
    │资源少✓  │    完整✓
    └─────────┘    可靠✓
                   版本锁定✓
```

---

### 🔧 CLAUDE 依赖管理决策规则

CLAUDE 应根据以下规则选择包管理工具：

| 场景 | 优先工具 | 命令示例 |
|------|---------|---------|
| **开发+测试** | UV | `uv pip install -e ".[dev,test]"` |
| **仅运行应用** | UV | `uv pip install -e "."` |
| **运行测试套件** | UV | `uv run pytest tests/ -v` |
| **发布到 PyPI** | Poetry | `poetry publish` |
| **完整依赖锁定** | Poetry | `poetry lock && poetry install` |
| **快速原型开发** | UV | `uv pip install <package>` |
| **CI/CD 流程** | UV | `uv pip install -e "."; uv run pytest` |
| **虚拟环境管理** | UV 内置 | `uv venv; source .venv/bin/activate` |
| **生产部署** | UV | `uv pip install -e "."`（然后 Docker/VM） |
| **依赖冲突解决** | Poetry | `poetry lock --no-update` |

---

### 🚀 快速命令参考

#### UV（推荐用于快速开发和测试）
```bash
# 一步完成：创建虚拟环境并安装所有依赖
uv venv && source .venv/bin/activate && uv pip install -e ".[dev,test]"

# 运行所有测试
uv run pytest tests/ -v

# 运行应用
uv run uvicorn src.main:app --reload

# 卸载依赖
uv pip uninstall <package-name>
```

#### Poetry（用于复杂项目和发布）
```bash
# 完整项目初始化
poetry install

# 运行测试
poetry run pytest tests/ -v

# 添加新依赖
poetry add <package-name>

# 生成 lock 文件
poetry lock
```

---

### ⚠️ 注意事项

1. **不要混用**：在同一个项目中不要同时使用 UV 和 Poetry，选择一个坚持使用
2. **虚拟环境**：UV 会自动管理虚拟环境，Poetry 需要手动激活
3. **.venv 位置**：确保在项目根目录创建虚拟环境，便于 IDE 识别
4. **CI/CD**：优先在 CI/CD 流程中使用 UV（更快更稳定）
5. **版本兼容性**：`pyproject.toml` 中的 `requires-python = ">=3.12"` 必须遵守

---

## 项目文档系统

Starting the Server：
Simply double-click the 
start_docs.bat
 file in your project root directory. Alternatively, run this command in your terminal:

 ```bash
 mkdocs serve -a 127.0.0.1:8008
 ```

Editing Documentation：
Navigate to the docs/ folder.Edit any Markdown (.md) file.Save the file.The browser page will automatically refresh to show your changes.